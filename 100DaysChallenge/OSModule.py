#OS Module
import os
# if (not os.path.exists("Data")):
#   os.mkdir("Data")
# for i in range(0,40):
#   os.mkdir (f"Data/Day r{i+1}")
#   os.rename(f"Data/Day {i+1}",f"Data/Tutorial {i+1}")

#Some Important Functions are Listdir and many more.
print(os.getcwd())
#remove(), rmdir(),
